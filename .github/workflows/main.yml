name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 15 minutes

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -s https://tradingbot-382l.onrender.com 
